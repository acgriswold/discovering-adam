---
import { Wordmark } from "@/components/global/wordmark";
import { storyblokEditable } from "@storyblok/astro";

const { blok } = Astro.props;
---

<Wordmark {...storyblokEditable(blok)} className="my-24 mt-16 md:mt-64" />